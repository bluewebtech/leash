<?php include '../inc/head.php' ?>

<h1>Getting Started | Configuration</h1>
<h2>General Configuration</h2>
<p>
	The main framework configuration file exists in <span class="path">/app/conf/config/</span>. 
	The config.php handles the configuration of settings such as the controller for the application 
	to load by default, the view to load by default once a controller has been called, and the 
	layout to load by default.
</p>
<p>
	Other config settings are the enabling and disabling of debugging, logging along with a few 
	logging settings such as the log storage location, log file names, and the defined extension 
	for all framework log files.
</p>
<p>
	Below describes each configuration setting:
</p>
<h2>Datasource Configuration</h2>
<h2>Mail Configuration</h2>

<?php include '../inc/foot.php' ?>